# Sprint 5: SSO Verification & Core Services

**Date:** 2026-06-03
**State:** plan

---

## Goal

End-to-end SSO validation for the 4 core C4 services (Chat/Meet/Mail/Office), verify nubus portal, and confirm backup integrity.

---

## Current State

- **57/57** pods Running across both namespaces.
- **k8up operator** deployed (1/1 Running). Manual backup test succeeded. Daily backup schedule active (01:22).
- **Clamav** 2/2 Running with proxy auth. **OpenProject** 6/6 Running with SECRET_KEY_BASE.
- **Portal** accessible at `portal.opendesk.hrz.uni-marburg.de` (HTTP 200 — Univention Portal HTML).
- **All 33 ingresses** in `opendesk-edu` namespace TLS-enabled on `*.opendesk.hrz.uni-marburg.de`.
- **5 ingresses** in `opendesk` namespace still on `*.opendesk-edu.org` (OX, Synapse, Jitsi, OpenCloud) — separate domain from edu deployment.
- **12 edu services** on /etc/hosts DNS override (awaiting external A records).
- **44 Keycloak clients** clean — no stale `opendesk.example.com` URIs.

---

## Tasks

### Task 1: SSO Login Verification (Browser)

Manually verify OIDC/SAML SSO works for 6 key services:
- **openproject** → https://projects.opendesk.hrz.uni-marburg.de — OIDC redirect
- **bookstack** → https://bookstack.opendesk.hrz.uni-marburg.de — OIDC redirect
- **etherpad** → https://etherpad.opendesk.hrz.uni-marburg.de — OIDC redirect
- **planka** → https://planka.opendesk.hrz.uni-marburg.de — OIDC redirect
- **n8n** → https://n8n.opendesk.hrz.uni-marburg.de — OIDC redirect
- **ssp** → https://ssp.opendesk.hrz.uni-marburg.de — OIDC redirect

For each: verify HTTP redirect chain → Keycloak login page → back to service.

**Files:**
- `helmfile/apps/*/values.yaml.gotmpl` — SSO config if misconfigured

**Verification:**
```bash
curl -sk -o /dev/null -w '%{redirect_url}' https://projects.opendesk.hrz.uni-marburg.de/
# Should return a Keycloak login URL
```

---

### Task 2: Core C4 Services Verification

Check the 4 core collaboration services respond and function:
- **Chat** (Synapse/Element): https://chat.opendesk.hrz.uni-marburg.de — HTTP 200, Matrix client loads
- **Meet** (Jitsi): https://meet.opendesk.hrz.uni-marburg.de — HTTP 200, Jitsi UI loads
- **Mail** (OX App Suite): https://webmail.opendesk.hrz.uni-marburg.de — HTTP 200, OX login loads
- **Office** (Collabora): https://office.opendesk.hrz.uni-marburg.de — HTTP 200, Collabora UI loads

**Note:** Chat, Meet, Mail ingresses use `opendesk-edu.org` domain — verify which domain they should use.

---

### Task 3: Portal Service Catalog Audit

Verify the nubus portal at `portal.opendesk.hrz.uni-marburg.de` lists all deployed edu services in the grid/launcher. Identify any missing entries.

**Files:**
- `helmfile/apps/nubus/values-nubus.yaml.gotmpl` — portal service definitions
- `helmfile/apps/portal-entries/values.yaml.gotmpl` — LDAP portal entries

---

### Task 4: Backup Integrity

Confirm the daily backup schedule produced a valid snapshot and test restoration capability:
```bash
kubectl get backup.k8up.io -n opendesk-edu
kubectl get snapshots.k8up.io -A | grep opendesk-edu
```

**Verification:** At least one snapshot exists in S3 for the edu namespace.

---

### Task 5: External DNS Documentation

Create a definitive reference of all missing external DNS A records needed for edu services. Output a shell-scriptable list for the DNS admin.

**Output:** shell script showing:
```bash
# DNS records needed for opendesk-edu
add_host() { echo "$1 IN A 192.168.3.201  ; $2"; }
add_host n8n     "n8n.opendesk.hrz.uni-marburg.de"
add_host code    "code.opendesk.hrz.uni-marburg.de"
# ...
```

---

## Blockers

- Core C4 services on `opendesk-edu.org` domain may need domain migration first
- Browser-based SSO verification requires interactive login — browser tool may not handle full Keycloak flow
- Portal requires LDAP users provisioned to show non-empty grid

## Out of Scope

- Production Helmfile sync (blocked by upstream v1.0.0-rc.12 bug)
- Git history redaction
- Domain migration of opendesk namespace services
