{{- if .Values.etherpad.monitoring.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "etherpad.fullname" . }}-monitor
  labels:
{{ include "etherpad.labels" . | nindent 4 }}
    {{- if .Values.etherpad.monitoring.labels }}
{{ toYaml .Values.etherpad.monitoring.labels | nindent 4 }}
    {{- end }}
spec:
  endpoints:
    - port: {{ .Values.etherpad.monitoring.port | default "http" | quote }}
      interval: {{ .Values.etherpad.monitoring.interval | default "30s" }}
      path: {{ .Values.etherpad.monitoring.path | default "/health" | quote }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "etherpad.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
{{- end }}
