apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "moodle.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "moodle.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.moodle.replicaCount }}
  selector:
    matchLabels:
      {{- include "moodle.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "moodle.selectorLabels" . | nindent 8 }}
        {{- with .Values.commonAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "moodle.serviceAccountName" . }}
      containers:
        - name: moodle
          image: "{{ .Values.moodle.image }}:{{ .Values.moodle.tag }}"
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          {{- with .Values.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: MOODLE_USERNAME
              value: {{ .Values.moodle.moodleUsername | quote }}
            - name: MOODLE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-moodle-secrets
                  key: moodle-password
            - name: MOODLE_EMAIL
              value: {{ .Values.moodle.moodleEmail | quote }}
            - name: MOODLE_SITE_NAME
              value: {{ .Values.moodle.moodleSiteName | quote }}
            - name: MOODLE_SKIP_INSTALL
              value: {{ .Values.moodle.moodleSkipInstall | quote }}
            - name: BITNAMI_DEBUG
              value: "false"
            - name: XML_CATALOG_FILES
              value: "/usr/share/xml/shibboleth/catalog.xml:/usr/share/xml/xmltooling/catalog.xml:/usr/share/xml/opensaml/saml20-catalog.xml:/usr/share/xml/opensaml/saml11-catalog.xml"
{{- if .Values.externalDatabase.host }}
            - name: MOODLE_DATABASE_TYPE
              value: {{ .Values.externalDatabase.type | default "pgsql" | quote }}
            - name: MOODLE_DATABASE_HOST
              value: {{ .Values.externalDatabase.host | quote }}
            - name: MOODLE_DATABASE_PORT
              value: {{ .Values.externalDatabase.port | toString | quote }}
            - name: MOODLE_DATABASE_NAME
              value: {{ .Values.externalDatabase.database | quote }}
            - name: MOODLE_DATABASE_USER
              value: {{ .Values.externalDatabase.user | quote }}
            - name: MOODLE_DATABASE_PASSWORD
              value: {{ .Values.externalDatabase.password | quote }}
{{- else }}
            - name: MOODLE_DATABASE_HOST
              value: "postgresql"
{{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: moodle-data
              mountPath: /bitnami/moodle
            - name: moodle-data
              mountPath: /var/www/moodledata
              subPath: moodledata
{{- if .Values.sso.saml.enabled }}
            - name: shibboleth-sp-config
              mountPath: /etc/shibboleth
              readOnly: true
            - name: apache-shibboleth-config
              mountPath: /etc/apache2/conf-enabled/shibboleth.conf
              subPath: shibboleth.conf
              readOnly: true
            - name: entity-metadata
              mountPath: /etc/shibboleth/metadata
              readOnly: true
{{- end }}
      volumes:
        - name: moodle-data
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-moodle-data
{{- if .Values.sso.saml.enabled }}
        - name: shibboleth-sp-config
          configMap:
            name: {{ .Release.Name }}-shibboleth-sp-config
        - name: apache-shibboleth-config
          configMap:
            name: {{ .Release.Name }}-apache-shibboleth-config
        - name: entity-metadata
          secret:
            secretName: {{ .Release.Name }}-entity-metadata
{{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
