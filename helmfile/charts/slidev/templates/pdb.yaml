{{- if .Values.slidev.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "slidev.fullname" . }}-pdb
  labels:
{{ include "slidev.labels" . | nindent 4 }}
spec:
  {{- if .Values.slidev.pdb.minAvailable }}
  minAvailable: {{ .Values.slidev.pdb.minAvailable }}
  {{- else }}
  maxUnavailable: {{ .Values.slidev.pdb.maxUnavailable }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "slidev.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
