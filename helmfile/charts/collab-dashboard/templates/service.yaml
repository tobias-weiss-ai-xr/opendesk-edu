apiVersion: v1
kind: Service
metadata:
  name: {{ include "collab-dashboard.fullname" . }}
  labels:
    {{- include "collab-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "collab-dashboard.selectorLabels" . | nindent 4 }}
