# CI values for BigBlueButton — minimal set for template rendering
bigbluebutton:
  greenlight:
    databaseUrl: "postgresql://greenlight:test@RELEASE-NAME-postgresql:5432/greenlight"
    secretKeyBase: "test-secret-key-for-ci"
