apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    {{- range $key, $value := .Values.bigbluebutton.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.bigbluebutton.ingress.className | quote }}
  rules:
    - host: {{ .Values.bigbluebutton.ingress.hosts.host | quote }}
      http:
        paths:
          - path: {{ .Values.bigbluebutton.ingress.hosts.paths.path | quote }}
            pathType: {{ .Values.bigbluebutton.ingress.hosts.paths.pathType | quote }}
            backend:
              service:
                name: {{ .Release.Name }}
                port:
                  number: {{ .Values.bigbluebutton.service.externalPort }}
  tls:
    - hosts:
        - {{ .Values.bigbluebutton.ingress.hosts.host | quote }}
      secretName: {{ .Values.bigbluebutton.ingress.tls.secretName | quote }}
