suite: Persistence
templates:
  - pvc.yaml
values:
  - ../ci/ci-values.yaml
tests:
  - it: should create a PersistentVolumeClaim
    asserts:
      - isKind:
          of: PersistentVolumeClaim

  - it: should set the correct storage size
    asserts:
      - equal:
          path: spec.resources.requests.storage
          value: "500Gi"

  - it: should set ReadWriteMany access mode
    asserts:
      - contains:
          path: spec.accessModes
          content: ReadWriteMany

  - it: should set storage class when configured
    set:
      bigbluebutton.volumes.recordings.storageClass: "ceph-cephfs-hdd-ec"
    asserts:
      - equal:
          path: spec.storageClassName
          value: "ceph-cephfs-hdd-ec"
