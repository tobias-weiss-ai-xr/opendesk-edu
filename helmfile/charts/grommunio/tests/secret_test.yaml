# SPDX-FileCopyrightText: 2026 openDesk Edu Contributors
# SPDX-License-Identifier: Apache-2.0
suite: test secret
templates:
  - secret.yaml
tests:
  - it: should create a Secret with sensitive data
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Secret
      - equal:
          path: type
          value: Opaque
      - equal:
          path: metadata.name
          value: RELEASE-NAME-grommunio-secret
  - it: should contain database password when set
    set:
      grommunio.db.password: test-db-password
    asserts:
      - equal:
          path: stringData.GROMMUNIO_DB_PASSWORD
          value: test-db-password
  - it: should contain cache password when set
    set:
      grommunio.cache.password: test-cache-password
    asserts:
      - equal:
          path: stringData.GROMMUNIO_CACHE_PASSWORD
          value: test-cache-password
  - it: should contain OIDC client secret when set
    set:
      grommunio.auth.oidc.clientSecret: test-oidc-secret
    asserts:
      - equal:
          path: stringData.GROMMUNIO_OIDC_CLIENT_SECRET
          value: test-oidc-secret
  - it: should auto-generate database password when not set
    asserts:
      - matchRegex:
          path: stringData.GROMMUNIO_DB_PASSWORD
          pattern: "^[a-zA-Z0-9]{32}$"
  - it: should auto-generate cache password when not set
    asserts:
      - matchRegex:
          path: stringData.GROMMUNIO_CACHE_PASSWORD
          pattern: "^[a-zA-Z0-9]{32}$"
  - it: should auto-generate OIDC client secret when not set
    asserts:
      - matchRegex:
          path: stringData.GROMMUNIO_OIDC_CLIENT_SECRET
          pattern: "^[a-zA-Z0-9]{32}$"
  - it: should auto-generate LDAP bind password when not set
    asserts:
      - matchRegex:
          path: stringData.GROMMUNIO_LDAP_BIND_PASSWORD
          pattern: "^[a-zA-Z0-9]{32}$"
  - it: should use explicit LDAP bind password when set
    set:
      grommunio.ldap.bindPassword: my-ldap-password
    asserts:
      - equal:
          path: stringData.GROMMUNIO_LDAP_BIND_PASSWORD
          value: my-ldap-password
