{{ $root := . }}

environments:
  edu:
    values:
      - environments/edu/secrets.yaml
      - environments/edu/ce-overrides.yaml.gotmpl
      - environments/edu/images.yaml
  edu-test:
    values:
      - environments/edu/test/values.yaml.gotmpl

repositories:
  - name: bitnami
    url: oci://registry-1.docker.io/bitnamicharts

helmfiles:
  # Auto-discover all edu app helmfiles via glob
  - path: apps/edu/*/helmfile.yaml.gotmpl
    values:
      - environments/edu/ce-overrides.yaml.gotmpl
