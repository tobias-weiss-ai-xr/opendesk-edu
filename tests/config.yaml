# openDesk Test Framework Configuration
# Environment: HRZ Production Cluster
# Domain: *.home.opendesk-edu.org

cluster:
  namespace: opendesk
  kubeconfig: ~/.kube/config

# Service endpoints to test
services:
  identity:
    keycloak:
      host: id.home.opendesk-edu.org
      realm: opendesk
      oidc_well_known: /.well-known/openid-configuration
      login_path: /realms/opendesk/protocol/openid-connect/auth

  portal:
    portal:
      host: portal.home.opendesk-edu.org
      paths:
        - /
        - /umc
        - /udm
    
  files:
    nextcloud:
      host: files.home.opendesk-edu.org
      oidc_client: nextcloud
      status_path: /status.php
    
    opencloud:
      host: opencloud.home.opendesk-edu.org
      oidc_client: opencloud
      graph_api: /graph

  communication:
    element:
      host: chat.home.opendesk-edu.org
      matrix_discovery: /.well-known/matrix/client
    
    synapse:
      host: matrix.home.opendesk-edu.org
      federation_path: /_matrix/federation
    
    jitsi:
      host: meet.home.opendesk-edu.org
    
    bigbluebutton:
      host: bbb.home.opendesk-edu.org
    
    intercom:
      host: ics.home.opendesk-edu.org

  groupware:
    ox_appsuite:
      host: webmail.home.opendesk-edu.org
      oidc_client: open-xchange
      api_paths:
        - /apisuite
        - /appsuite/api
    
    ox_dav:
      host: dav.home.opendesk-edu.org
      paths:
        - /caldav
        - /carddav

  collaboration:
    collabora:
      host: office.home.opendesk-edu.org
      wopi_discovery: /hosting/discovery
    
    cryptpad:
      host: pad.home.opendesk-edu.org
    
    drawio:
      host: draw.home.opendesk-edu.org
    
    excalidraw:
      host: excalidraw.home.opendesk-edu.org
    
    etherpad:
      host: etherpad.home.opendesk-edu.org
    
    slidev:
      host: slides.home.opendesk-edu.org
    
    ttyd:
      host: term.home.opendesk-edu.org
    
    openproject:
      host: projects.home.opendesk-edu.org
      oidc_client: openproject
    
    collab_dashboard:
      host: tools.home.opendesk-edu.org

  lms:
    ilias:
      host: lms.home.opendesk-edu.org
      saml_metadata: /Shibboleth.sso/Metadata
    
    moodle:
      host: moodle.home.opendesk-edu.org
      oidc_client: moodle

  knowledge:
    xwiki:
      host: wiki.home.opendesk-edu.org
      saml_metadata: /xwiki/bin/loginsaml/saml/request

  infrastructure:
    minio:
      host: objectstore-ui.home.opendesk-edu.org
    
    seaweedfs_ui:
      host: objectstorage-ui.home.opendesk-edu.org
    
    seaweedfs_s3:
      host: objectstorage.home.opendesk-edu.org
    
    grafana:
      host: grafana.home.opendesk-edu.org
    
    root:
      host: home.opendesk-edu.org
      paths:
        - /.well-known
        - /

# Thresholds
thresholds:
  ssl_expiry_days: 30
  http_timeout: 10
  retry_attempts: 2
  
# Allowed HTTP status codes (others = fail)
allowed_http_codes:
  - 200
  - 302
  - 401
  - 403
  
# Critical services (fail if any of these fail)
critical_services:
  - identity.keycloak
  - portal.portal
  - files.nextcloud
  - files.opencloud

# Paths to skip in smoke tests
skip_paths:
  - /metrics
  - /healthz
  - /readyz
  - /livez